# Standard cibuildwheel workflow, copied directly from
# <https://cibuildwheel.pypa.io/en/v3.0.1/ci-services/>

name: Build

on: [push, pull_request]

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # macos-13 is an intel runner, macos-14 is apple silicon
        # Note: Windows ARM64 (windows-11-arm) isn't actually supported by cibuildwheel's
        # Meson backend—it only wires up setuptools and setuptools_rust on ARM, and its
        # VS-toolchain detection doesn't configure cl.exe for Meson builds. Skipping this
        # platform until official ARM64 Meson support lands.
        os: [ubuntu-latest, ubuntu-24.04-arm, windows-latest, macos-13, macos-14]

    steps:
      - uses: actions/checkout@v4

      - name: Install libomp and set up environment
        if: runner.os == 'macOS'
        run: |
          brew install libomp llvm

          LLVM_PREFIX=$(brew --prefix llvm)
          LIBOMP_PREFIX=$(brew --prefix libomp)

          # Tell setuptools which compiler to call
          echo "CC=${LLVM_PREFIX}/bin/clang"    >> $GITHUB_ENV
          echo "CXX=${LLVM_PREFIX}/bin/clang++" >> $GITHUB_ENV

          # Point -fopenmp at the right headers + libs
          echo "CPPFLAGS=-I${LIBOMP_PREFIX}/include" >> $GITHUB_ENV
          echo "LDFLAGS=-L${LIBOMP_PREFIX}/lib"       >> $GITHUB_ENV

          # Prepend Homebrew's LLVM to PATH so any clang/clang++ calls hit the right one
          echo "PATH=${LLVM_PREFIX}/bin:$PATH"      >> $GITHUB_ENV

      - name: Figure out macOS deployment target
        if: runner.os == 'macOS'
        run: |
          # e.g. "13.4.1" → "13.4"
          OS_VER=$(sw_vers -productVersion | cut -d. -f1-2)
          echo "MACOSX_DEPLOYMENT_TARGET=${OS_VER}" >> $GITHUB_ENV

      - name: Build wheels on normal platforms
        uses: pypa/cibuildwheel@v3.0.1
        # with:
        #   package-dir: .
        #   output-dir: wheelhouse
        #   config-file: "{package}/pyproject.toml"

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  test_wheels:
    name: Test wheels on ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    needs: build_wheels
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform: ubuntu-latest
            python-cmd: python
            setup-python: true
          - os: ubuntu-24.04-arm
            platform: ubuntu-24.04-arm
            python-cmd: python
            setup-python: true
          - os: windows-latest
            platform: windows-latest
            python-cmd: python
            setup-python: true
          - os: macos-13
            platform: macos-13
            python-cmd: python
            setup-python: true
          - os: macos-14
            platform: macos-14
            python-cmd: python
            setup-python: true
          - os: ubuntu-latest
            platform: alpine-musllinux
            container: python:3.11-alpine
            python-cmd: python
            setup-python: false

    container: ${{ matrix.container }}

    steps:
      - name: Install system dependencies (Alpine only)
        if: matrix.platform == 'alpine-musllinux'
        run: |
          apk add --no-cache \
            bash \
            git \
            build-base \
            libffi-dev \
            openssl-dev \
            curl

      - uses: actions/checkout@v4

      - name: Set up Python (non-Alpine)
        if: matrix.setup-python
        uses: actions/setup-python@v5
        with:
          python-version: |
            3.8
            3.9
            3.10
            3.11
            3.12
            3.13

      - name: Install script dependencies
        run: |
          ${{ matrix.python-cmd }} -m pip install --upgrade pip
          ${{ matrix.python-cmd }} -m pip install -r scripts/requirements.txt

      - name: Download all wheel artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./all-wheels

      - name: Organize wheels
        shell: bash
        run: |
          # Create a flat directory with all wheels
          mkdir -p ./wheels-flat
          find ./all-wheels -name "*.whl" -exec cp {} ./wheels-flat/ \;
          echo "Wheels organized:"
          ls -la ./wheels-flat/

      - name: Test all wheels
        run: |
          ${{ matrix.python-cmd }} scripts/test_wheels.py --wheels-dir ./wheels-flat --results-dir ./wheel-results

      - name: Generate summary for this platform
        run: |
          ${{ matrix.python-cmd }} scripts/generate_summary.py --results-dir ./wheel-results --github-summary

      - name: Upload wheel test results
        uses: actions/upload-artifact@v4
        with:
          name: wheel-test-results-${{ matrix.platform }}
          path: ./wheel-results/

  performance_summary:
    name: Performance Summary
    runs-on: ubuntu-latest
    needs: [test_wheels]
    if: always()
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install script dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt

      - name: Download all wheel test results
        uses: actions/download-artifact@v4
        with:
          pattern: wheel-test-results-*
          path: ./all-results
          merge-multiple: true

      - name: Generate consolidated summary
        run: |
          python scripts/generate_summary.py --results-dir ./all-results --github-summary

      - name: Upload consolidated performance summary
        uses: actions/upload-artifact@v4
        with:
          name: performance-summary
          path: ./all-results/
